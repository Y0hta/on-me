<header>
  <nav class="navbar navbar-expand-md navbar-dark bd-navbar fixed-top">
    <div class="w-100">
    </div>
    <div class="mx-auto order-0">
      <% if user_signed_in? %>
        <a class="navbar-brand mx-auto font-weight-bold" href="/dashboard">
          On Me
        </a>
      <% else %>
        <a class="navbar-brand mx-auto font-weight-bold" href="<%= root_path %>">
          On Me
        </a>
      <% end %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    </div>
    <div class="collapse navbar-collapse w-100">
      <ul class="navbar-nav ml-auto">
        <!-- Common -->
        <li class="nav-item active">
          <a class="nav-link" href="/explore">
            探す
          </a>
        </li>
        <!-- User section -->
        <% if user_signed_in? %>
          <li class="nav-item active">
            <a class="nav-link" href="<%= user_path(current_user) %>">
              MyPage
            </a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-warning" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= current_user.name %>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
              <a class="dropdown-item d-inline-flex" href="/dashboard">
                <i class="material-icons mr-1">dashboard</i>ダッシュボード
              </a>
              <div class="dropdown-divider"></div>
              <%= link_to destroy_user_session_path, method: :delete, class: "dropdown-item d-inline-flex" do %>
            <i class="material-icons mr-1">exit_to_app</i><span>ログアウト</span>
          <% end %>
            </div>
          </li>
        <% else %>
          <li class="nav-item active">
            <a class="nav-link" href="<%= new_user_session_path %>">
              LOGIN
            </a>
          </li>
          <li class="nav-item active">
            <a class="nav-link text-warning" href="<%= new_user_registration_path %>">
              SIGN UP
            </a>
          </li>
        <% end %>
      </ul>
    </div>
  </nav>
</header>

